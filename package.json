{
  "name": "jeopardy-local-multiplayer",
  "version": "2.0.0",
  "private": true,
  "description": "A local-network multiplayer Jeopardy-style game for hosts and players.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check lib/game-store.js && node --check lib/room-manager.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "5.2.1",
    "socket.io": "4.8.3"
  },
  "devDependencies": {
    "socket.io-client": "4.8.3"
  }
}
